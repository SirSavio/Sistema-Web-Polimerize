import React from 'react';
// import {useHistory} from 'react-router-dom';

export default function RedirectUser(){
    return(
        <div>
            <button>Consultar Amostra</button>
            <button>Administração</button>
        </div>
    );
}